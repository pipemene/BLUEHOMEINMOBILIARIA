# Puerto del servidor Express
PORT=8080

# Credenciales de servicio de Google
GOOGLE_CLIENT_EMAIL=tu-servicio@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nABC123...\n-----END PRIVATE KEY-----\n"
# Asegúrate de compartir la hoja con el correo del servicio anterior (solo lectura/edición según lo requerido).

# Configuración de la hoja de cálculo
GOOGLE_SHEETS_SPREADSHEET_ID=ID_DE_TU_HOJA
GOOGLE_SHEETS_USERS_RANGE=Usuarios!A:D
GOOGLE_SHEETS_ORDERS_RANGE=Ordenes!A:K

# Seguridad y CORS
# Si se define INTERNAL_API_KEY, el endpoint /api/debug/sheets exigirá el header `x-internal-api-key` con ese valor.
INTERNAL_API_KEY=
# Si defines FRONTEND_ORIGIN (o varias separadas por coma), el CORS se limitará a esos orígenes. Ejemplos:
# FRONTEND_ORIGIN=https://tu-frontend.rw.app
# FRONTEND_ORIGIN=https://tu-frontend.rw.app,http://localhost:8080
FRONTEND_ORIGIN=
